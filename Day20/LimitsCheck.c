#include <stdio.h>
#include <limits.h>
int main()
{

    unsigned int min,max;
    min = 0;
    max = UINT_MAX;
    printf("Minimum value of unsigned int : %d\d",min);
    printf("Maximum value of unsigned int : %u",max);

    return 0;
}
